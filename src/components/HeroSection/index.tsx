import Action from './Action'
import Body from './Body'
import Heading from './Heading'
import Root from './Root'

export const HeroSection = {
  Action,
  Body,
  Heading,
  Root,
}

/* export default function HeroSection(props: HeroSectionProps) {
  return (
    <div className="lg:2/6 mt-20 text-left lg:ml-16 lg:mt-40 xl:w-2/4">
      <div className="text-6xl font-semibold leading-none">{props.heading}</div>
      <div className="text-true-gray-500 my-6 text-xl font-light antialiased">
        You are about to experiencing an awesome planning session.
      </div>
      <Button
        asChild
        className="rounded-full bg-gradient-to-b from-blue-600 to-blue-700 px-8 py-4 font-normal tracking-wide text-white outline-none transition duration-200 ease-in-out hover:from-blue-700 hover:shadow-lg focus:outline-none"
      >
        <a href="">Create your free voting room</a>
      </Button>
    </div>
  )
} */
